import { TestBed } from '@angular/core/testing';

//import { SoporteGuardGuard } from './soporte-guard.guard';
/*
describe('SoporteGuardGuard', () => {
  let guard: SoporteGuardGuard;

  beforeEach(() => {
    TestBed.configureTestingModule({});
    guard = TestBed.inject(SoporteGuardGuard);
  });

  it('should be created', () => {
    expect(guard).toBeTruthy();
  });
});*/
